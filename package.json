{
  "name": "adfvalidation",
  "version": "1.0.0",
  "description": "Validates files of ADF in a given location, returning warnings or errors.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Aaron Ye",
  "license": "ISC",
  "dependencies": {
    "azure-pipelines-task-lib": "^3.1.0"
  }
}
